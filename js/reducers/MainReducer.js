
const initialState = {
    fetching: false
}

export function MainReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}


export default MainReducer;
